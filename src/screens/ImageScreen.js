import {Text,Button,Platform, Pressable,Image,View, ImageComponent} from 'react-native'

const HomeScreen = ({navigation}) => (
    <View style={{flex:1,justifyContent:'center',alignItems:'center'}}>
        <ImageComponent/>
    </View>
)

export default HomeScreen